"use client";
import { useLiveData } from "@/hooks/useLiveData";
import { Icon } from "./Icon";
export function SunCard({ date }: { date: string }) { const { weather } = useLiveData(date); return <section aria-label="Napkelte és napnyugta Villasimiusban" className="mx-5 grid grid-cols-[1fr_auto_1fr] items-center gap-3 border-b border-deep-sea/10 px-[3px] pb-2.5 pt-3"><div className="flex items-center gap-2 text-sm font-semibold"><Icon name="sunrise" size={19} className="text-coral" /><span>{weather?.sunrise ?? "—"}</span></div><p className="text-[11px] font-semibold tracking-[.02em] text-deep-sea/55">Villasimius</p><div className="flex items-center justify-end gap-2 text-sm font-semibold"><Icon name="sunset" size={19} className="text-coral" /><span>{weather?.sunset ?? "—"}</span></div></section>; }
